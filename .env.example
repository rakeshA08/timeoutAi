# CV Studio Environment Configuration

# Database URL (SQLite default in local runtime directory)
DATABASE_URL="file:./.cvstudio/cvstudio.db"

# Data Storage Root Directory
DATA_ROOT="./.cvstudio"

# Next.js Port
PORT=3000
